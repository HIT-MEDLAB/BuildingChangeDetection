# Backend API URL
# Do NOT include a trailing /api here — every request in src/pages/*.jsx and
# src/api.js already prefixes its path with /api (e.g. api.post("/api/auth/login")).
# Adding /api here too produces /api/api/... 404s (see bug report, 27 Jul).
VITE_API_URL=http://localhost:3000
