// TODO: Implement processing status view
// - Show a loading/progress indicator while ML inference runs
// - Poll GET /api/inspections/:id to check status
// - Redirect to /results when status becomes "completed"
// - Show error state if processing fails

function ProcessingPage() {
  return (
    <div>
      <h1>Processing</h1>
      <p>TODO: Implement processing status indicator with polling.</p>
    </div>
  );
}

export default ProcessingPage;
