// TODO: Implement image upload
// - Two file inputs: "before" image and "after" image
// - Preview the selected images before uploading
// - Call POST /api/inspections/upload with multipart form data
// - Navigate to /processing on successful upload
// - Handle errors (file too large, wrong format, etc.)

function UploadPage() {
  return (
    <div>
      <h1>Upload Images</h1>
      <p>TODO: Implement dual-image upload form — see docs/api-spec.md for the endpoint details.</p>
    </div>
  );
}

export default UploadPage;
