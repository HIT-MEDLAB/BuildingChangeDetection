// TODO: Implement login form
// - Email and password fields
// - Call POST /api/auth/login via the api instance
// - Store the JWT token (localStorage, context, or state management — your choice)
// - Redirect to /upload on success
// - Show error message on failure

function LoginPage() {
  return (
    <div>
      <h1>Login</h1>
      <p>TODO: Implement login form — see docs/api-spec.md for the endpoint details.</p>
    </div>
  );
}

export default LoginPage;
